namespace Injector_UI
{
    public enum InjectionResult
    {
        Success,
        ProcessNotFound,
        AccessDenied,
        ArchitectureMismatch,
        InjectionFailed,
        ModuleNotFound,
        InitializationFailed,
        Timeout
    }
}
